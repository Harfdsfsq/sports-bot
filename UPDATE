# UPDATE

## Сделано сейчас

- Настроена дневная политика top-5: цель 5 прогнозов в день, hard cap 7.
- Для ручного запуска `run-bot` добавлен добор до 5 прогнозов за один прогон, если дневная цель ещё не закрыта.
- По логам запуска 29.04.2026 проверено, что quota-часть уже работает: `allowed_this_run=5`, `is_manual_run=true`.
- Исправлена причина, из-за которой прогнозы всё ещё не публиковались: финальный proxy single-source фильтр требовал confidence 73.5%, хотя лучшие кандидаты дня имели 64-69% при сильном EV и edge.
- Для режима top-5 снижены только reserve-пороги Tier B/proxy: EV от 6.0%, edge от 3.0 п.п., confidence от 64.0%; Tier C остаётся выключенным.
- Для target-5 расширены допустимые Tier B рынки: totals, DNB, team totals, BTTS и spreads. Это закрывает частую причину отказа `family_not_allowed:spreads`.
- DNB outlier guard оставлен включённым, но для режима top-5 лимиты сделаны реалистичнее, чтобы не срезать все proxy-кандидаты с двумя линиями.
- Сохранены жёсткие страховки: минимум 2 линии для Telegram, запрет proxy single-book, обязательное market confirmation для proxy, финальный EV/edge guard.
- Добавлено базовое логирование CLI и основного `PredictionRunner`.
- Добавлен корневой `README.md` с запуском, workflow и ключевыми файлами.
- Репозиторий очищен от сгенерированных `__pycache__` и `*.pyc`.

## Что проверить сегодня

1. Запустить GitHub Actions workflow `run-bot` вручную с `volume_mode=target_5`.
2. После выполнения открыть artifact `run-bot-current-*`.
3. Проверить:
   - `.data/exports/latest-daily-top5-publish-policy.json`;
   - `.data/exports/latest-controlled-fallback-report.json`;
   - `.data/exports/latest-run-summary.json`;
   - Telegram: должно быть до 5 прогнозов, если кандидаты проходят финальные guards.
4. Если прогнозов меньше 5, смотреть `reject_reasons` в `latest-controlled-fallback-report.json`. Теперь это должно означать реальную нехватку качественных кандидатов, а не конфликт между top-5 политикой и финальными overrides.

## Ближайшие действия

1. Добавить unit-тесты для `apply_daily_top5_publish_policy.py`: ручной запуск, scheduled-запуск, hard cap, уже закрытая дневная цель.
2. Вынести расчёт дневной квоты в общий модуль, чтобы `apply_volume_policy.py` и `apply_daily_top5_publish_policy.py` не дублировали подсчёт опубликованных прогнозов.
3. Улучшить покрытие Totals/BTTS/spreads через контекстные провайдеры и matching матчей между источниками.
4. Разбить `PredictionRunner` на отдельные шаги: settlement, bootstrap, offers, context, candidates, quality, export, publish.
5. Добавить CI-проверку стиля и типов после стабилизации текущего релиза.
6. Перевести историю запусков и состояние из JSON в SQLite/Postgres или сделать SQLite основным runtime-хранилищем.
7. Построить регулярный ML/quality цикл на `training-dataset`: backtest, калибровка вероятностей, контроль ROI/CLV по сегментам.

## Операционное правило

Бот должен стремиться к 4-5 прогнозам в день, но не публиковать слабые ставки ради числа. Если top-5 не набрался, сначала улучшаем покрытие данных, matching и калибровку порогов, а не отключаем финальные guards.
