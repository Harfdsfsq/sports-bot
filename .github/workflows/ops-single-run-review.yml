name: Ops • single run review

on:
  workflow_dispatch:

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Review placeholder
        run: |
          echo "Use the internal-pipeline bundle artifact from the main workflow."
