# Ограничение

Этот пакет делает внешний strict publication gate поверх текущего бота.
Он не переписывает внутреннюю математику `app/services/*`.
Следующий шаг после нового bundle — уже точечный патч внутренних сервисов.
