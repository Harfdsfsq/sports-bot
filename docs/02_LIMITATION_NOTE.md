# Ограничение

Это не полная слепая перепись `app/services/model.py`.
Патч добавляет canonicalization и reject-слой поверх текущего pipeline, чтобы следующий точный патч в `model.py` делать уже по чистому следу.
