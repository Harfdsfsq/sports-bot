# Local replace notes

The archive is packaged with repository-relative folders at the root:

- `.github/`
- `config/`
- `scripts/`
- `docs/`

That means you can extract it **on top of the local repository root** and then push through GitHub Desktop.
